import { StaffManagementPanel } from "#/components/admin/StaffManagementPanel";

/** Admin-tab wrapper around the shared live-floor staff panel. */
export function AdminStaffManagement() {
	return <StaffManagementPanel />;
}
